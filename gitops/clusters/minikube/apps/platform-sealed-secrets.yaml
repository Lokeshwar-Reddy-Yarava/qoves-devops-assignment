apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-sealed-secrets
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Lokeshwar-Reddy-Yarava/qoves-devops-assignment.git
    targetRevision: HEAD
    path: gitops/manifests/platform/sealed-secrets
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      # Do not use ServerSideApply: on Kubernetes 1.35 Argo CD (structured merge)
      # fails comparison with:
      #   .status.terminatingReplicas: field not declared in schema
      # That surfaces as Sync Status = Unknown even while Health is Healthy.
